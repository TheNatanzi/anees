# Handoff — overnight build 2026-09-04 → 2026-09-05

One line: **all 8 milestones built and tested (64 tests green, Codex + council gates in plan/OVERNIGHT-LOG.md); one item is
PARTIAL (live hourly Doc fetch needs a publish-to-web URL from you); nothing was sent to Amal; both links are minted and waiting.**

## What shipped (DONE / PARTIAL / BLOCKED)
| M | Piece | Status | Proof |
|---|---|---|---|
| M0 | Data spine: Supabase project `anees` (free org, ref yljcbdxvnkfrwvelypfu), 8 tables + RLS, Doc → 2,120 words, loose Arabizi matcher (55/57 right, 0 wrong), hourly import task | DONE / **PARTIAL** (hourly job runs but re-reads the saved snapshot: no unattended Google credential on this PC) | tests/test_m0_* (10) |
| M1 | Lesson understanding on Aug 25 + Sep 4: Doc-word events with prompted / correction / asked / elicited flags, typed-chat location 46/47 family (41/47 exact form), topic ranking, shared clips ≤ 25 s, speaker-label floor | DONE | tests/test_m1_understand.py (12) |
| M2 | Report pages + rich emails (chart + log) for both lessons, numbers = stored rows, "–" with reason when unknown | DONE (council 8/10) | tests/test_m2_report.py (13); emails in your inbox 07:14 UTC |
| M3 | Amal's planner link: 3 questions + 8 Doc-checked sentences, keep/drop/edit → rules, resume, expiry | DONE | tests/test_m3_planner.py (3); stand-in 11 screens in 12 s |
| M4 | Amal's after-lesson link: 5 questions with 15-s clips, verdicts re-score the exact event row, expiry, already-done | DONE (council 8/10) | tests/test_m4_after.py (4); stand-in 15 screens in 16 s |
| M5 | Flashcards, Quizlet mode: flip, Got it / Missed, wrong-pile replay until empty, Arabic/English first, shuffle, subjects, 3× weighting, ice-cold rule, offline queue | DONE | tests/test_m5_cards.py (7) |
| M6 | Homework sheet (≤ 10 items, Doc words only) on the after link; edits change the next sheet | DONE | in test_m4_after.py |
| M7 | One site: Today · Lessons · Words (search 20/20 < 9 ms) · Flashcards · Amal (rules log) · Grammar · Future projects; Lighthouse mobile 91 | DONE | tests/test_m7_index.py (4) |
| M8 | Retries, budget ledger + 90 % stop, failure email, guarded post-process, morning checklist, this handoff | DONE | tests/test_m8_pipeline.py (11); checklist executed once (log) |

BLOCKED: nothing. PARTIAL: only the live hourly Doc fetch (above). Two Codex audits (M0, M5, M7) were still "verifying" when this
was written; their findings, if any, are in the log or will be picked up by the next chat.

## Costs (ceiling prices, ledger data/budget.json)
ElevenLabs 0.00 USD (every lesson tonight reused cached transcripts). OpenAI 0.44 USD (5 calls: gpt-5.4-mini rejected, gpt-5.5 ×4 for sentence suggestions).

## The message to Amal (paste as-is, 11:00–11:30, only after the checklist passes)
> Hi Amal — before today's lesson, could you tap through this 2-minute page? It suggests a topic and 8 practice sentences; keep, drop or edit anything, your choice always wins: <paste the plan.html link from data/amal_links.json>

After the lesson (evening), the second link:
> Thank you for today! Five quick questions about the lesson (one tap each, with the audio) plus homework suggestions you can keep or drop: <paste the after.html link from data/lessons/pipeline.log or data/amal_links.json>

## Next 3 actions
0. **NEW 2026-09-05 12:40 — M9 (WhatsApp chat as second source)** is specified in plan/OVERNIGHT-BUILD-2026-09-05.md §3; export sits in data/whatsapp/raw (git-ignored). Not built. Blocked on Medi's grill answer for M9c.
1. **Publish the Doc to the web** (Doc → File → Share → Publish to web) and give Claude the link → hourly import goes live (until then Claude re-exports on request).
2. **Send Amal the planner link at 11:00–11:30**, then after the 13:30 lesson wait for the two emails and send the after link in the evening. Her first 5 verdicts are what raises every council score above 8.
3. **Record two tracks next lesson** (Craig on Discord, or Ennuicastr if Amal is on her phone): the only real fix for merged voices; then re-run `python scripts/understand_lesson.py <date>` and the tutor-reaction experiment.

## Where things are
- Site: https://thenatanzi.github.io/anees/ · reports: /lessons/<date>-report.html · cards: /cards.html · Amal pages: /amal/plan.html, /amal/after.html (token required)
- Data: Supabase `anees` (keys in User env ANEES_SUPABASE_*; the anon key is public by design, RLS protects rows). Migrations in supabase/migrations (001-004, applied).
- Log of every gate, Codex finding and council: plan/OVERNIGHT-LOG.md · checklist: plan/MORNING-CHECKLIST.md
- Rules that hold: never contact Amal from code (send_lesson_email.mjs hard-codes thenatanzi@); the Doc is never written; no guessed number ("–" + reason); never push with failing tests; lesson audio never deleted.

## Added after the final Codex audit (07:20)
- 31 test/temporary Amal tokens sit in two old commits of the public repo (the file was untracked afterwards). All 31 rows were already deleted from Supabase → the tokens open nothing (verified by SQL: 0 alive). The two live links were minted later and are not in git. If you prefer, Claude can rewrite history + force-push in a future chat.
- Open P1 by design: the flashcards page inserts card results with the public anon key (no login). Anyone with the site URL could post fake results. Fix planned: a personal token for Medi's pages (same mechanism as Amal's links).
- Codex's M0 audit never finished in its sandbox; every M0 hard limit (Doc read-only, Medi-only email, secrets, audio) was re-verified clean by the full-repo audit.

## Morning session additions (08:30 → 10:30) — read this first in the next chat
- Checklist ran 15/15 (08:34). Links: `data/amal_links.json` (before 2026-09-05, after 2026-09-04; both re-minted, fresh).
- **Miss classifier v2** (`scripts/miss_kind.py`, tests/test_m9_miss_kind.py): kinds word / choice / article / gender / tense / plural /
  pronunciation / unclear; phrase slips sit on the head word (aktar ishi); superlative pattern; dangling-el- rule; chat-typed words
  anchor Latin ASR spellings (Nahl → Na7el) and break matcher ties; Amal's after-link buttons are Right · Wrong word · Wrong grammar · Not Medi.
- **'new' bucket**: heard in the last 3 lessons and not yet drilled (3 first-try rights on 2 days); never mixed with missed; first on the
  report with a practice button; cards default to the New set (`cards.html?subject=b-new`).
- Data audit: 56 test card rows, 72 stale word_stats rows, 96 test flag rules purged; recompute self-cleans; dates in local day.
- UI: one centered menu on every page; out-of-date banner (build stamp on commit/publish, 60-s check, flashing Reload bar).
- Pending: Codex UI/UX audit job `task-mtokizrg-aqwvt5` (read with `node "%USERPROFILE%\.claude\plugins\cache\openai-codex\codex\1.0.6\scripts\codex-companion.mjs" result task-mtokizrg-aqwvt5`);
  the stuck M0 audit job task-mto11mpt-5acq7s can be ignored.
- Known weakness to fix next: homographs (بعيد = "I repeat" here, but the Doc only has Ba3eed = Far); the P1 "anon can insert card_results".
- Next lesson 13:30 today: the hourly task will transcribe, report, and mint the after link (URL in data/lessons/pipeline.log, grep after_link).
